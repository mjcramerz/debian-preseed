# Managed by unattended-installer.

[Service]
# fwupdmgr uses 2 when metadata is already current and 101 when no supported
# hardware is available. Both are successful refresh outcomes.
SuccessExitStatus=2 101
