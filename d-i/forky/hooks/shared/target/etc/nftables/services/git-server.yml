apiVersion: cybops.nftables/v1
kind: NftablesServiceOverlay
metadata:
  name: git-server
  description: Git SSH server overlay using SSH transport.
  merge_priority: 50
services:
  git_ssh:
    enabled: true
    direction: inbound
    protocols:
    - tcp
    ports:
    - 22
    allow:
      ipv4:
      - 10.8.0.0/24
      ipv6:
      - fd00:8::/64
      interfaces:
      - eth0
      - en*
      - __INSTALLER_MANAGED_NETWORK_ETHERNET_IFACE__
      - wlan0
      - wl*
      - __INSTALLER_MANAGED_NETWORK_WIFI_IFACE__
      - wg0
    sensitive: true
    rate_limit: 20/minute burst 40 packets
