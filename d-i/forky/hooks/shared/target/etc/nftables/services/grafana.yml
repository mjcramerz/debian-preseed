apiVersion: cybops.nftables/v1
kind: NftablesServiceOverlay
metadata:
  name: grafana
  description: Grafana web UI overlay.
  merge_priority: 50
services:
  grafana:
    enabled: true
    direction: inbound
    protocols:
    - tcp
    ports:
    - 3000
    allow:
      ipv4:
      - 10.8.0.0/24
      ipv6:
      - fd00:8::/64
      interfaces:
      - eth0
      - en*
      - __INSTALLER_MANAGED_NETWORK_ETHERNET_IFACE__
      - wlan0
      - wl*
      - __INSTALLER_MANAGED_NETWORK_WIFI_IFACE__
      - wg0
    sensitive: true
