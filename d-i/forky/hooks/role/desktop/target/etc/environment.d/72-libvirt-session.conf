# Managed by unattended-installer.
# The local unprivileged libvirtd session starts only through virtops or the managed
# virt-manager launcher, never as an implicit client side effect.
LIBVIRT_AUTOSTART=0
