# Managed by unattended-installer.
#
# The administrator-managed user unit starts a persistent unprivileged
# libvirtd instance explicitly. Keep its diagnostics in journald so the
# managed rsyslog policy can retain them without console forwarding.
log_outputs = "3:journald"
