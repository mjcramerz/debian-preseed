# Managed by unattended-installer.
#
# Debian's explicit split driver packages are loaded by this libvirtd daemon.
# Keep daemon diagnostics in journald so rsyslog can route them into the
# managed libvirt log without copying records to a console.
log_outputs = "3:journald"
