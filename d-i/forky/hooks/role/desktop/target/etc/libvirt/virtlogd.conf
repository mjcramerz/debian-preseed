# Managed by unattended-installer.
log_outputs = "3:journald"
# Debian's package logrotate rule uses 2097153 bytes so virtlogd must rotate
# first and keep that fallback rule a no-op while logd is active.
max_size = 2097152
max_backups = 4
